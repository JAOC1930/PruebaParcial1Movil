<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PruebaParcial" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Splash" parent="Base.Theme.PruebaParcial">
        <item name="android:windowBackground">@drawable/logocitysplash</item>
    </style>

    <style name="Theme.PruebaParcial" parent="Base.Theme.PruebaParcial" />

    <style name="Theme.PruebaParcial.Fullscreen" parent="Theme.PruebaParcial">
        <item name="android:actionBarStyle">@style/Widget.Theme.PruebaParcial.ActionBar.Fullscreen
        </item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.PruebaParcial.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>
</resources>